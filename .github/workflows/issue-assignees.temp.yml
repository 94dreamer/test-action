name: Issue Add Assigness

on:
  issues:
    types: [opened, edited]

jobs:
  call-issue-assigness:
    uses: 94dreamer/test-action/.github/workflows/issue-assignees.yml@master
    with:
      project_name: ${{github.event.repository.name}}
      issue_title: ${{github.event.issue.title}}
